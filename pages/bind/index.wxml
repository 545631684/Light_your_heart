<block wx:if="{{$taroCompReady}}">
  <view class="page-bind">
    <view class="page-bind-title">手机快捷登录</view>
      <at-form>
        <at-input compid="{{$compid__48}}">
          <block>
            <block wx:if="{{ableSend}}">
              <text bindtap="anonymousFunc1">获取验证码</text>
            </block>
            <block wx:else>
              <text>{{countdown}}s后重新获取</text>
            </block>
          </block>
        </at-input>
        <at-input compid="{{$compid__49}}"></at-input>
        <at-button compid="{{$compid__50}}">登录</at-button>
      </at-form>
    </view>
  </block>
